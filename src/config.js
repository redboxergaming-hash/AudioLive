export const DELAY_PRESETS = [5, 10, 15, 20];
export const DEFAULT_DELAY_SECONDS = 10;
export const FINE_TUNE_STEPS = [-1, -0.5, 0.5, 1];
export const STORAGE_KEYS = {
  pageUrl: 'audio-delay:last-url',
  delaySeconds: 'audio-delay:last-delay'
};
export const DIAGNOSTIC_LIMIT = 60;
